import Metadata, { EditTabContentType } from '@/pages/app/appliance/Metadata/Analysis';

const MetadataAnalysis = () => {
  return <Metadata contentType={EditTabContentType.ANALYSIS} />;
};

export default MetadataAnalysis;
