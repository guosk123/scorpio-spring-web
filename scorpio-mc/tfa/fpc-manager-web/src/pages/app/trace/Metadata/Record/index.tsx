import Metadata, { EditTabContentType } from '@/pages/app/appliance/Metadata/Analysis';

const MetadataRecord = () => {
  return <Metadata contentType={EditTabContentType.RECORD} />;
};

export default MetadataRecord;
